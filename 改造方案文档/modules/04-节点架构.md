# 节点架构

> **版本：** v2.0（三层架构）
> **状态：** 设计完成
> **更新日期：** 2025-01-10

[← 返回总览](../00-总览与导航.md)

---

## 📋 目录

- [核心理念](#核心理念)
- [三层节点架构](#三层节点架构)
- [节点加载机制](#节点加载机制)
- [平台节点管理](#平台节点管理)
- [用户自定义节点](#用户自定义节点)
- [安全机制](#安全机制)
- [数据库设计](#数据库设计)

---

## 💡 核心理念

### 架构调整说明

**v2.0 重要调整：** 删除用户端 npm 安装功能，改为管理员统一管理。

### 为什么删除用户 npm 安装？

| 原因 | 说明 |
|------|------|
| **安全风险** | 用户安装任意 npm 包存在严重安全隐患 |
| **自动转换不可靠** | 社区节点结构复杂，自动凭证转换成功率低 |
| **汉化工作量巨大** | 2000+ 社区节点无法统一汉化 |
| **资源浪费** | 多个工作空间重复安装相同节点 |
| **对标 Coze** | Coze 不允许用户安装插件，只能使用平台提供的 |

### 新架构核心原则

1. **管理员添加社区节点**：从 npm 挑选优质节点，手动改造（凭证转换 + 汉化）后上传
2. **用户需要节点可以**：
   - ① 自己上传代码（立即可用，仅自己工作空间）
   - ② 联系管理员添加（管理员评估后添加到平台节点）
3. **VM2 沙箱隔离**：所有数据库加载的节点都在沙箱中运行
4. **审核机制**：用户可提交自定义节点审核，通过后成为平台节点

---

## 🔌 三层节点架构

### 架构概览

```
┌─────────────────────────────────────────────────────────────┐
│  多租户 SaaS 平台 - 三层节点架构（v2.0）                      │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  Layer 1: 内置基础节点（53个）                               │
│  ┌─────────────────────────────────────────────────────┐    │
│  │ 📁 文件系统加载（只读）                              │    │
│  │ Set, If, Code, Switch, Merge, Filter...             │    │
│  │ 源码位置: packages/nodes-base/nodes/*/*.node.ts     │    │
│  │ 加载方式: DirectoryLoader                           │    │
│  │ 无凭证依赖，纯逻辑节点                               │    │
│  │ 所有租户共享，无需配置                               │    │
│  └─────────────────────────────────────────────────────┘    │
│                           ↓                                  │
│  Layer 2: 平台节点（管理员添加，所有用户可用）               │
│  ┌─────────────────────────────────────────────────────┐    │
│  │ 💾 数据库加载: platform_node 表                      │    │
│  │ 🔒 VM2 沙箱执行                                      │    │
│  │                                                      │    │
│  │ 2.1 平台托管节点（Platform Managed）                │    │
│  │     ├─ OpenAI Chat（平台 API Key + 按量计费）       │    │
│  │     ├─ Claude Chat（平台 API Key + 按量计费）       │    │
│  │     ├─ Gemini Chat（平台 API Key + 按量计费）       │    │
│  │     └─ 用户无需配置，开箱即用                        │    │
│  │                                                      │    │
│  │ 2.2 第三方节点（Third Party）                       │    │
│  │     ├─ GitHub（用户配置自己的 Token）               │    │
│  │     ├─ Slack（用户配置自己的 Token）                │    │
│  │     ├─ Telegram（用户配置自己的 Bot Token）         │    │
│  │     └─ 管理员从 npm 精选、手动改造、汉化            │    │
│  │                                                      │    │
│  │ 加载方式: PlatformNodesService.loadAll()            │    │
│  │ 可用范围: 所有用户可见                               │    │
│  │ 维护者: 管理员统一管理                               │    │
│  └─────────────────────────────────────────────────────┘    │
│                           ↓                                  │
│  Layer 3: 用户自定义节点（User Custom）                     │
│  ┌─────────────────────────────────────────────────────┐    │
│  │ 💾 数据库加载: custom_node 表                        │    │
│  │ 🔒 VM2 沙箱隔离执行                                  │    │
│  │                                                      │    │
│  │ 用户编写 TypeScript 代码上传                         │    │
│  │ 仅自己工作空间可见                                   │    │
│  │ 可选提交审核 → 通过后变成 Layer 2                   │    │
│  │                                                      │    │
│  │ 加载方式: CustomNodesService.loadWorkspaceNodes()   │    │
│  │ 可用范围: 仅上传者工作空间                           │    │
│  │ 维护者: 用户自己                                     │    │
│  └─────────────────────────────────────────────────────┘    │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

### 层级对比

| 特性 | Layer 1（内置） | Layer 2（平台） | Layer 3（自定义） |
|------|----------------|----------------|------------------|
| **数量** | 53个固定 | 管理员管理 | 无限制 |
| **存储位置** | 文件系统 | 数据库 | 数据库 |
| **加载方式** | DirectoryLoader | 数据库 + VM2 | 数据库 + VM2 |
| **可见范围** | 所有租户 | 所有用户 | 仅自己工作空间 |
| **添加者** | 平台开发者 | **管理员** | 用户 |
| **汉化** | ✅ 统一汉化 | ✅ 管理员汉化 | 用户自己处理 |
| **凭证转换** | 无需 | ✅ 管理员手动 | 用户自己处理 |

---

## 🔄 节点加载机制

### 核心加载服务

```typescript
// packages/cli/src/load-nodes-and-credentials.ts

@Service()
export class LoadNodesAndCredentials {
  /**
   * 获取所有可用节点（根据工作空间）
   */
  async getAllNodeTypes(workspaceId?: string): Promise<INodeTypeNameIndex> {
    // 1. 加载内置节点（53个，从文件系统）
    const builtinNodes = await this.loadBuiltinNodes();

    // 2. 加载平台节点（从 platform_node 表，管理员添加）
    const platformNodes = await this.loadPlatformNodes();

    // 3. 加载工作空间自定义节点（从 custom_node 表，用户上传）
    const customNodes = workspaceId
      ? await this.loadCustomNodes(workspaceId)
      : {};

    // 4. 验证并合并（优雅降级）
    return this.validateAndMerge(builtinNodes, platformNodes, customNodes);
  }

  /**
   * 加载内置节点（Layer 1）
   */
  private async loadBuiltinNodes(): Promise<INodeTypeNameIndex> {
    // 使用原有的 DirectoryLoader
    return await this.directoryLoader.loadBuiltinNodes();
  }

  /**
   * 加载平台节点（Layer 2 - 管理员添加）
   */
  private async loadPlatformNodes(): Promise<INodeTypeNameIndex> {
    const nodeTypes: INodeTypeNameIndex = {};

    try {
      // 查询所有启用的平台节点
      const platformNodes = await this.platformNodeService.findAll({
        isEnabled: true,
      });

      for (const node of platformNodes) {
        try {
          // 编译节点代码（VM2 沙箱）
          const NodeClass = this.compileNodeCode(node.nodeCode);
          const nodeInstance = new NodeClass();

          this.validateNodeMetadata(nodeInstance.description);

          nodeTypes[node.nodeKey] = {
            type: nodeInstance,
            sourcePath: `platform://${node.id}`,
            source: 'platform',
            nodeSource: node.nodeSource, // 'platform_managed' | 'third_party'
          };
        } catch (error) {
          this.logger.warn(`Failed to load platform node ${node.nodeKey}:`, error);
        }
      }
    } catch (error) {
      this.logger.error('Failed to load platform nodes:', error);
    }

    return nodeTypes;
  }

  /**
   * 加载工作空间自定义节点（Layer 3 - 用户上传）
   */
  private async loadCustomNodes(workspaceId: string): Promise<INodeTypeNameIndex> {
    const nodeTypes: INodeTypeNameIndex = {};

    try {
      // 查询该工作空间的所有自定义节点
      const customNodes = await this.customNodeService.getWorkspaceNodes(workspaceId);

      for (const node of customNodes) {
        try {
          // 编译节点代码（VM2 沙箱）
          const NodeClass = this.compileNodeCode(node.nodeCode);
          const nodeInstance = new NodeClass();

          this.validateNodeMetadata(nodeInstance.description);

          nodeTypes[node.nodeKey] = {
            type: nodeInstance,
            sourcePath: `custom://${node.id}`,
            source: 'custom',
          };
        } catch (error) {
          this.logger.warn(`Failed to load custom node ${node.nodeKey}:`, error);
        }
      }
    } catch (error) {
      this.logger.error('Failed to load custom nodes:', error);
    }

    return nodeTypes;
  }

  /**
   * 编译节点代码（安全沙箱）
   */
  private compileNodeCode(code: string): any {
    const { VM } = require('vm2');
    const vm = new VM({
      timeout: 5000,
      sandbox: {
        require: this.createSandboxRequire(),
        console,
        Buffer,
      },
      eval: false,
      wasm: false,
    });

    const compiledCode = vm.run(code);

    if (typeof compiledCode !== 'function') {
      throw new Error('Node code must export a class');
    }

    return compiledCode;
  }

  /**
   * 创建安全的 require 函数
   */
  private createSandboxRequire() {
    const allowedModules = ['n8n-workflow', 'n8n-core'];

    return (moduleName: string) => {
      if (!allowedModules.includes(moduleName)) {
        throw new Error(`Module ${moduleName} is not allowed`);
      }
      return require(moduleName);
    };
  }

  /**
   * 重新加载平台节点（管理员添加/更新后调用）
   */
  async reloadPlatformNodes() {
    // 清除缓存
    this.platformNodesCache = {};

    // 重新加载
    await this.loadPlatformNodes();

    this.logger.info('Platform nodes reloaded');
  }

  /**
   * 重新加载工作空间节点（用户上传/删除后调用）
   */
  async reloadWorkspaceNodes(workspaceId: string) {
    // 清除缓存
    delete this.customNodesCache[workspaceId];

    // 重新加载
    await this.getAllNodeTypes(workspaceId);

    this.logger.info(`Custom nodes reloaded for workspace ${workspaceId}`);
  }
}
```

---

## 🌐 平台节点管理

### 管理员添加流程

```
管理员从 npm 挑选优质节点
    ↓
下载到本地
    ↓
手动改造：
  - 凭证转换（credentials → properties）
  - 汉化（displayName, description 等）
  - 测试验证
    ↓
上传到管理员后台
    ↓
选择节点类型：
  - 平台托管（配置平台 API Key + 计费规则）
  - 第三方（用户自己配置认证）
    ↓
启用 → 所有用户可见
```

### 管理员 API

```typescript
// packages/cli/src/modules/platform-nodes/platform-nodes.controller.ts

@RestController('/admin/platform-nodes')
@Authorized(['global:admin'])
export class AdminPlatformNodesController {
  constructor(
    private platformNodeService: PlatformNodesService,
    private loadNodesAndCredentials: LoadNodesAndCredentials,
    private push: Push,
  ) {}

  /**
   * 管理员上传平台节点
   */
  @Post('/')
  async uploadPlatformNode(req: AuthenticatedRequest) {
    const {
      nodeKey,
      nodeName,
      nodeSource, // 'platform_managed' | 'third_party'
      nodeCode,
      nodeDefinition,
      npmPackageName,
      npmVersion,
      platformApiKey, // 如果是 platform_managed
      pricingConfig,  // 如果是 platform_managed
    } = req.body;

    // 1. 验证节点代码
    await this.nodeSecurityService.validateNodeCode(nodeCode);

    // 2. 如果是平台托管，验证 API Key
    if (nodeSource === 'platform_managed') {
      if (!platformApiKey) {
        throw new BadRequestError('平台托管节点必须配置 API Key');
      }
      // 加密 API Key
      const encryptedKey = await this.encryption.encrypt(platformApiKey);
    }

    // 3. 创建平台节点
    const platformNode = await this.platformNodeService.create({
      nodeKey,
      nodeName,
      nodeSource,
      nodeCode,
      nodeDefinition,
      npmPackageName,
      npmVersion,
      platformApiKeyEncrypted: nodeSource === 'platform_managed' ? encryptedKey : null,
      pricingConfig: nodeSource === 'platform_managed' ? pricingConfig : null,
      createdBy: req.user.id,
      isEnabled: true,
    });

    // 4. 触发全局节点重新加载
    await this.loadNodesAndCredentials.reloadPlatformNodes();

    // 5. WebSocket 通知所有用户
    this.push.sendToAll({
      type: 'reloadNodeType',
      data: { nodeKey }
    });

    return platformNode;
  }

  /**
   * 管理员更新平台节点
   */
  @Patch('/:nodeId')
  async updatePlatformNode(req: AuthenticatedRequest) {
    const { nodeId } = req.params;
    const updateData = req.body;

    const node = await this.platformNodeService.findById(nodeId);

    if (!node) {
      throw new NotFoundError('节点不存在');
    }

    // 更新节点
    const updatedNode = await this.platformNodeService.update(nodeId, updateData);

    // 重新加载
    await this.loadNodesAndCredentials.reloadPlatformNodes();

    // 通知所有用户
    this.push.sendToAll({
      type: 'reloadNodeType',
      data: { nodeKey: updatedNode.nodeKey }
    });

    return updatedNode;
  }

  /**
   * 管理员启用/禁用节点
   */
  @Patch('/:nodeId/toggle')
  async toggleNode(req: AuthenticatedRequest) {
    const { nodeId } = req.params;
    const { isEnabled } = req.body;

    await this.platformNodeService.update(nodeId, { isEnabled });
    await this.loadNodesAndCredentials.reloadPlatformNodes();

    this.push.sendToAll({
      type: 'reloadNodeType',
      data: { nodeId, isEnabled }
    });

    return { success: true };
  }

  /**
   * 管理员删除节点
   */
  @Delete('/:nodeId')
  async deleteNode(req: AuthenticatedRequest) {
    const { nodeId } = req.params;

    await this.platformNodeService.delete(nodeId);
    await this.loadNodesAndCredentials.reloadPlatformNodes();

    this.push.sendToAll({
      type: 'removeNodeType',
      data: { nodeId }
    });

    return { success: true };
  }

  /**
   * 获取所有平台节点
   */
  @Get('/')
  async getAllPlatformNodes() {
    return await this.platformNodeService.findAll();
  }
}
```

---

## 👤 用户自定义节点

### 用户需求流程

```
用户需要某个节点
  ↓
【方案1】自己编写
  ├─ 编写 TypeScript 代码
  ├─ 上传到"我的节点"
  ├─ 立即可用（仅自己工作空间）
  └─ （可选）提交审核 → 全平台可用

【方案2】联系管理员
  ├─ 提交节点请求
  ├─ 管理员评估优先级
  ├─ 管理员改造上传
  └─ 全员可用
```

### 用户 API

```typescript
// packages/cli/src/modules/custom-nodes/custom-nodes.controller.ts

@RestController('/custom-nodes')
export class CustomNodesController {
  constructor(
    private customNodeService: CustomNodesService,
    private nodeSecurityService: NodeSecurityService,
    private loadNodesAndCredentials: LoadNodesAndCredentials,
    private push: Push,
  ) {}

  /**
   * 用户上传自定义节点代码
   */
  @Post('/upload')
  @GlobalScope('customNode:create')
  async uploadCustomNode(req: AuthenticatedRequest) {
    const { nodeKey, nodeName, nodeCode, nodeDefinition } = req.body;
    const workspaceId = req.workspaceContext.workspaceId;

    // 1. 检查工作空间节点数量限制（例如：50个）
    const nodeCount = await this.customNodeService.getWorkspaceNodeCount(workspaceId);
    if (nodeCount >= 50) {
      throw new BadRequestError('工作空间节点数量已达上限（50个）');
    }

    // 2. 安全验证
    await this.nodeSecurityService.validateNodeCode(nodeCode);

    // 3. 检查节点 key 是否重复
    const existing = await this.customNodeService.findByKey(workspaceId, nodeKey);
    if (existing) {
      throw new BadRequestError(`节点 ${nodeKey} 已存在`);
    }

    // 4. 创建自定义节点
    const customNode = await this.customNodeService.create({
      workspaceId,
      nodeKey,
      nodeName,
      nodeSource: 'user_upload',
      nodeCode,
      nodeDefinition,
      createdBy: req.user.id,
      reviewStatus: 'draft', // 初始状态
    });

    // 5. 触发工作空间节点重新加载
    await this.loadNodesAndCredentials.reloadWorkspaceNodes(workspaceId);

    // 6. WebSocket 通知前端
    this.push.sendToWorkspace(workspaceId, {
      type: 'reloadNodeType',
      data: { nodeKey }
    });

    return customNode;
  }

  /**
   * 提交审核（用户希望节点成为平台节点）
   */
  @Post('/:nodeId/submit-review')
  @GlobalScope('customNode:update')
  async submitForReview(req: AuthenticatedRequest) {
    const { nodeId } = req.params;
    const workspaceId = req.workspaceContext.workspaceId;

    const node = await this.customNodeService.findById(nodeId);

    if (!node || node.workspaceId !== workspaceId) {
      throw new NotFoundError('节点不存在');
    }

    if (node.reviewStatus !== 'draft') {
      throw new BadRequestError('节点已提交审核');
    }

    await this.customNodeService.update(nodeId, {
      reviewStatus: 'pending',
    });

    return { success: true, message: '提交审核成功，等待管理员审核' };
  }

  /**
   * 删除自定义节点
   */
  @Delete('/:nodeId')
  @GlobalScope('customNode:delete')
  async deleteNode(req: AuthenticatedRequest) {
    const { nodeId } = req.params;
    const workspaceId = req.workspaceContext.workspaceId;

    const node = await this.customNodeService.findById(nodeId);

    if (!node || node.workspaceId !== workspaceId) {
      throw new NotFoundError('节点不存在');
    }

    await this.customNodeService.delete(nodeId);
    await this.loadNodesAndCredentials.reloadWorkspaceNodes(workspaceId);

    this.push.sendToWorkspace(workspaceId, {
      type: 'removeNodeType',
      data: { nodeKey: node.nodeKey }
    });

    return { success: true };
  }

  /**
   * 获取工作空间的所有自定义节点
   */
  @Get('/')
  @GlobalScope('customNode:list')
  async getWorkspaceNodes(req: AuthenticatedRequest) {
    const workspaceId = req.workspaceContext.workspaceId;
    return await this.customNodeService.getWorkspaceNodes(workspaceId);
  }
}
```

### 审核 API

```typescript
// packages/cli/src/modules/custom-nodes/node-review.controller.ts

@RestController('/admin/node-review')
@Authorized(['global:admin'])
export class NodeReviewController {
  /**
   * 获取待审核节点列表
   */
  @Get('/pending')
  async getPendingReviews() {
    return await this.customNodeService.findAll({
      reviewStatus: 'pending',
    });
  }

  /**
   * 审核通过（将自定义节点升级为平台节点）
   */
  @Post('/:nodeId/approve')
  async approveNode(req: AuthenticatedRequest) {
    const { nodeId } = req.params;

    const customNode = await this.customNodeService.findById(nodeId);

    if (!customNode || customNode.reviewStatus !== 'pending') {
      throw new BadRequestError('节点不存在或状态不正确');
    }

    // 1. 创建平台节点（复制数据）
    const platformNode = await this.platformNodeService.create({
      nodeKey: customNode.nodeKey,
      nodeName: customNode.nodeName,
      nodeSource: 'third_party', // 用户提交的默认为第三方
      nodeCode: customNode.nodeCode,
      nodeDefinition: customNode.nodeDefinition,
      createdBy: req.user.id,
      isEnabled: true,
    });

    // 2. 更新自定义节点状态
    await this.customNodeService.update(nodeId, {
      reviewStatus: 'approved',
      reviewedBy: req.user.id,
      reviewedAt: new Date(),
    });

    // 3. 重新加载平台节点
    await this.loadNodesAndCredentials.reloadPlatformNodes();

    // 4. 通知所有用户
    this.push.sendToAll({
      type: 'reloadNodeType',
      data: { nodeKey: platformNode.nodeKey }
    });

    return { success: true, platformNode };
  }

  /**
   * 审核拒绝
   */
  @Post('/:nodeId/reject')
  async rejectNode(req: AuthenticatedRequest) {
    const { nodeId } = req.params;
    const { reviewNote } = req.body;

    await this.customNodeService.update(nodeId, {
      reviewStatus: 'rejected',
      reviewNote,
      reviewedBy: req.user.id,
      reviewedAt: new Date(),
    });

    return { success: true };
  }
}
```

---

## 🔒 安全机制

### 节点代码安全验证

```typescript
// packages/cli/src/modules/custom-nodes/node-security.service.ts

@Service()
export class NodeSecurityService {
  /**
   * 验证节点代码安全性
   */
  async validateNodeCode(code: string): Promise<void> {
    // 1. 禁止使用危险模块
    const dangerousModules = [
      'child_process',
      'fs',
      'path',
      'os',
      'process',
      'eval',
      'Function',
    ];

    for (const module of dangerousModules) {
      if (code.includes(module)) {
        throw new Error(`节点代码包含危险模块: ${module}`);
      }
    }

    // 2. 禁止使用危险函数
    const dangerousFunctions = [
      'eval(',
      'Function(',
      'require(',
      '__dirname',
      '__filename',
    ];

    for (const func of dangerousFunctions) {
      if (code.includes(func)) {
        throw new Error(`节点代码包含危险函数: ${func}`);
      }
    }

    // 3. 代码长度限制（1MB）
    if (code.length > 1024 * 1024) {
      throw new Error('节点代码超过大小限制（1MB）');
    }

    // 4. 尝试编译验证语法
    try {
      const ts = require('typescript');
      const result = ts.transpileModule(code, {
        compilerOptions: {
          module: ts.ModuleKind.CommonJS,
          target: ts.ScriptTarget.ES2020,
        },
      });

      if (result.diagnostics && result.diagnostics.length > 0) {
        throw new Error('节点代码存在语法错误');
      }
    } catch (error) {
      throw new Error(`节点代码编译失败: ${error.message}`);
    }
  }
}
```

### VM2 沙箱配置

```typescript
// 严格的沙箱环境
const vm = new VM({
  timeout: 5000, // 5秒超时
  sandbox: {
    // 只允许访问白名单模块
    require: (moduleName: string) => {
      const allowed = ['n8n-workflow', 'n8n-core'];
      if (!allowed.includes(moduleName)) {
        throw new Error(`模块 ${moduleName} 不允许使用`);
      }
      return require(moduleName);
    },
    console,
    Buffer,
  },
  // 禁止访问外部变量
  eval: false,
  wasm: false,
  fixAsync: false,
});
```

### 配额限制

| 限制项 | 默认值 | 说明 |
|--------|--------|------|
| 每工作空间节点数 | 50 | 防止滥用 |
| 节点代码大小 | 1MB | 防止过大文件 |
| 节点执行超时 | 30秒 | 防止死循环 |
| 节点内存限制 | 512MB | 防止内存泄漏 |

---

## 📊 数据库设计

### platform_node 表（平台节点）

```sql
CREATE TABLE platform_node (
  -- 主键
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),

  -- 基本信息
  node_key VARCHAR(100) NOT NULL UNIQUE,
  node_name VARCHAR(200) NOT NULL,
  version VARCHAR(50) DEFAULT '1.0.0',

  -- 节点来源（关键字段）
  node_source VARCHAR(50) NOT NULL CHECK (
    node_source IN ('platform_managed', 'third_party')
  ),

  -- npm 信息（如果来源于 npm）
  npm_package_name VARCHAR(200),
  npm_version VARCHAR(50),
  npm_author VARCHAR(200),

  -- 节点定义和代码
  node_definition JSONB NOT NULL,
  node_code TEXT NOT NULL,

  -- 平台托管节点字段
  platform_api_key_encrypted TEXT,  -- 平台 API Key（加密）
  pricing_config JSONB,  -- 计费配置

  -- 状态
  is_enabled BOOLEAN DEFAULT true,

  -- 元数据
  description TEXT,
  icon_url VARCHAR(500),
  documentation_url VARCHAR(500),

  -- 创建信息
  created_by UUID NOT NULL REFERENCES "user"(id),
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_platform_node_source ON platform_node(node_source);
CREATE INDEX idx_platform_node_enabled ON platform_node(is_enabled);
```

### custom_node 表（用户自定义节点）

```sql
CREATE TABLE custom_node (
  -- 主键
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),

  -- 关联信息
  workspace_id UUID NOT NULL REFERENCES project(id) ON DELETE CASCADE,

  -- 基本信息
  node_key VARCHAR(100) NOT NULL,
  node_name VARCHAR(200) NOT NULL,
  version VARCHAR(50) DEFAULT '1.0.0',

  -- 节点来源（固定为 user_upload）
  node_source VARCHAR(50) NOT NULL DEFAULT 'user_upload',

  -- 节点定义和代码
  node_definition JSONB NOT NULL,
  node_code TEXT NOT NULL,

  -- 安全配置
  sandbox_enabled BOOLEAN DEFAULT true,
  sandbox_timeout INTEGER DEFAULT 30000,
  code_signature TEXT,

  -- 审核状态
  review_status VARCHAR(50) DEFAULT 'draft' CHECK (
    review_status IN ('draft', 'pending', 'approved', 'rejected')
  ),
  review_note TEXT,
  reviewed_by UUID REFERENCES "user"(id),
  reviewed_at TIMESTAMP,

  -- 创建信息
  created_by UUID NOT NULL REFERENCES "user"(id),
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,

  -- 唯一约束：工作空间内节点 key 唯一
  UNIQUE(workspace_id, node_key)
);

CREATE INDEX idx_custom_node_workspace ON custom_node(workspace_id);
CREATE INDEX idx_custom_node_review_status ON custom_node(review_status);
```

### 废弃表清理

```sql
-- 删除原有社区节点相关表（无需迁移）
DROP TABLE IF EXISTS installed_nodes;
DROP TABLE IF EXISTS installed_packages;
```

---

## ✅ 总结

### 核心优势

| 优势 | 说明 |
|------|------|
| **安全性** | ✅ 只有管理员可添加社区节点，VM2 沙箱隔离 |
| **质量保证** | ✅ 管理员手动改造、汉化，质量有保证 |
| **用户体验** | ✅ 开箱即用，统一汉化，无需配置（平台托管） |
| **灵活性** | ✅ 用户可自己上传节点，立即使用 |
| **可扩展性** | ✅ 用户可贡献节点，审核后全平台共享 |
| **运维简化** | ✅ 统一管理，无重复安装 |

### 实施要点

1. **删除原有社区节点功能**（用户端 npm 安装）
2. **实现管理员节点管理**（手动上传、改造）
3. **实现用户自定义节点**（上传、审核）
4. **严格的安全验证**（代码扫描 + VM2 沙箱）
5. **审核机制**（用户贡献 → 管理员审核 → 平台节点）

### 对标 Coze

- ✅ 平台统一提供节点（类似 Coze 插件）
- ✅ 用户无需配置（平台托管节点）
- ✅ 质量有保证（管理员审核）
- ✅ 开箱即用（统一汉化）

---

[← 上一章：数据库设计](./03-数据库设计.md) | [返回总览](../00-总览与导航.md) | [下一章：AI服务架构 →](./05-AI服务架构.md)
