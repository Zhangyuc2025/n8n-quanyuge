# 凭证系统移除 - 后端清理清单

> **策略：** 删除节点执行引擎中的凭证注入和验证逻辑
> **预计工时：** 0.5 天（4 小时）
> **复杂度：** 低

---

## 🎯 清理目标

删除后端中所有凭证相关的：
1. ✅ Entity、Repository、Service、Controller（已完成）
2. 🔄 节点执行上下文中的凭证注入
3. 🔄 工作流执行前的凭证验证
4. 🔄 错误处理中的凭证相关逻辑

---

## 📋 需要清理的文件

### 1. 节点执行上下文 (packages/core)

| 文件 | 问题 | 修改方式 | 工时 |
|------|------|---------|------|
| `packages/core/src/execution-engine/node-execution-context/index.ts` | `getCredentials()` 方法 | 删除整个方法 | 0.5h |
| `packages/core/src/node-execution-functions.ts` | `getCredentials()` 实现 | 删除实现 | 0.5h |

---

### 2. 错误处理清理 (packages/core)

| 文件 | 问题 | 修改方式 | 工时 |
|------|------|---------|------|
| `packages/core/src/errors/index.ts` | 凭证相关错误类 | ✅ 已删除 | - |

---

### 3. 类型定义清理

| 文件 | 问题 | 修改方式 | 工时 |
|------|------|---------|------|
| `packages/workflow/src/Interfaces.ts` | `INodeCredentials` 类型 | 删除类型定义 | 0.3h |

---

## 🔍 查找所有后端凭证引用

```bash
# 查找 packages/core 中的凭证引用
grep -r "getCredentials\|INodeCredentials" packages/core/src \
  --include="*.ts" \
  --exclude-dir=node_modules

# 查找 packages/workflow 中的凭证类型
grep -r "INodeCredentials\|credentials\?" packages/workflow/src \
  --include="*.ts"
```

---

## 📊 工时总计

| 任务 | 工时 |
|------|------|
| 节点执行上下文 | 1h |
| 类型定义清理 | 0.3h |
| 测试修复 | 0.5h |
| **总计** | **1.8h ≈ 0.5 天** |

---

**创建日期：** 2025-01-10
**维护者：** 开发团队
